#include <iostream>
#include <cstdio>
#include <vector>
#include <algorithm>
#include <set>
#include <cmath>

#define ll long long int
#define pll pair<long long, long long>
#define pii pair<int, int>
#define pb push_back
#define mp make_pair
#define getchar_unlocked getchar
#define F first
#define S second
#define MOD 1000000007

using namespace std;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
	int column_count = 0;
	cout << "Enter number of columns:\n";
	cin >> column_count;
	map <string, map<string, int> > count_map;
	for(int i=0; i<
	return 0;
}

string read_till_comma() {
	int c;
	while((c=getchar())!= ',' && c!='\n' && c!=EOF
}
